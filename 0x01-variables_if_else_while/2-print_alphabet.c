#include <stdlib.h>

/**
 * main - prints the alphabetic
 * RETURN: Always (Sucess)
 *
 */

int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++
	putchar(letter);
	putchar('\n');
	return (0);
}
